[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["frontend", "data/raw", "data/processed"]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]  # line length handled loosely during scaffold stage
