# Install with: pip install -r requirements-dev.txt --break-system-packages
# (pulls in requirements.txt too)
-r requirements.txt

pytest>=8.2
pytest-cov>=5.0
httpx>=0.27          # needed for FastAPI's TestClient
ruff>=0.4            # lint + format
mypy>=1.10
types-Pillow
