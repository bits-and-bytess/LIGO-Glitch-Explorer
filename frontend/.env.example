# Copy to .env.local for local dev overrides, or set VITE_API_BASE
# directly in Vercel's project environment variables for production.
#
# Leave unset locally to use the /api dev proxy defined in vite.config.js.
VITE_API_BASE=https://your-backend-host.example.com
